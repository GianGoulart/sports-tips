{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Sportstips API",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/server",
            "envFile": "${workspaceFolder}/.env",
            "cwd": "${workspaceFolder}",
            "showLog": true
        }
    ]
}